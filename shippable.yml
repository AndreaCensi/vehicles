language: python
reset_minion: true

python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  - sudo apt-get install cmake
  - sudo apt-get install python-numpy python-scipy libblas-dev liblapack-dev gfortran
  - pip install scipy 
  - pip install -r requirements.txt
  - python setup.py develop
  - "cd src/raytracer && ./install.sh"

script:
  - nosetests vehicles
