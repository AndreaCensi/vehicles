language: python
reset_minion: true


python:
  - 2.7
  # - 3.2
  # - 3.3
  # - 3.4
  # - pypy
  
install:
  - pip install -r requirements.txt
  - "cd src/raytracer && ./install.sh"
  
branches:
  only:
    - master
    - env_fault

script:
  - nosetests vehicles
